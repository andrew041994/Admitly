import { type FormEvent, useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { createEvent } from '../lib/userApi';
function iso(value: string) { return new Date(value).toISOString(); }
export function CreateEventPage() { const navigate = useNavigate(); const [form, setForm] = useState({ title: '', description: '', start: '', end: '', venue: '', address: '', tier: 'General Admission', price: '0', quantity: '100' }); const [error, setError] = useState<string | null>(null); const [busy, setBusy] = useState(false); async function submit(e: FormEvent) { e.preventDefault(); setBusy(true); setError(null); try { const event = await createEvent({ title: form.title, short_description: form.description, start_at: iso(form.start), end_at: iso(form.end), timezone: Intl.DateTimeFormat().resolvedOptions().timeZone || 'America/Guyana', custom_venue_name: form.venue, custom_address_text: form.address, ticket_tiers: [{ name: form.tier, price_amount: form.price, currency: 'GYD', quantity_total: Number(form.quantity), min_per_order: 1, max_per_order: 10 }] }); navigate(`/my-events/${event.id}`, { replace: true }); } catch (reason) { setError(reason instanceof Error ? reason.message : 'Unable to create event.'); } finally { setBusy(false); } } return <section className="user-page narrow-page"><p className="eyebrow">Creator workspace</p><h1>Create Event</h1><div className="verification-notice"><strong>Before publication</strong><p>Event creators must be 18+. Send government-issued ID separately using Admitly’s verification process. ID is not uploaded through this website or stored in the application.</p></div><form className="panel web-form two-column-form" onSubmit={submit}>{error ? <p className="form-error form-wide">{error}</p> : null}<label className="form-wide">Event title<input value={form.title} onChange={(e) => setForm({ ...form, title: e.target.value })} required /></label><label className="form-wide">Short description<textarea value={form.description} onChange={(e) => setForm({ ...form, description: e.target.value })} /></label><label>Starts<input type="datetime-local" value={form.start} onChange={(e) => setForm({ ...form, start: e.target.value })} required /></label><label>Ends<input type="datetime-local" value={form.end} onChange={(e) => setForm({ ...form, end: e.target.value })} required /></label><label>Venue name<input value={form.venue} onChange={(e) => setForm({ ...form, venue: e.target.value })} required /></label><label>Address<input value={form.address} onChange={(e) => setForm({ ...form, address: e.target.value })} /></label><label>Ticket tier<input value={form.tier} onChange={(e) => setForm({ ...form, tier: e.target.value })} required /></label><label>Price (GYD)<input type="number" min="0" step="0.01" value={form.price} onChange={(e) => setForm({ ...form, price: e.target.value })} required /></label><label>Quantity<input type="number" min="1" value={form.quantity} onChange={(e) => setForm({ ...form, quantity: e.target.value })} required /></label><button className="form-wide" disabled={busy}>{busy ? 'Creating…' : 'Create draft event'}</button></form></section>; }
